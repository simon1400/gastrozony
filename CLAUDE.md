# Gastrozony — инструкции проекта

## Хранение
**Вся память, заметки и временные файлы этого проекта — только внутри `D:\gastrozony`.**
Память: `.claude/memory/`. Документация: `docs/`.
Никогда не писать в `D:\creditas\ibk` — это другой (несвязанный) проект.

## В начале сессии прочитать
1. `.claude/memory/MEMORY.md` — статус проекта
2. `.claude/memory/brief.md` — ТЗ заказчика
3. `docs/brainstorm.md` — архитектура и открытые вопросы

## Структура
- `client/` — фронтенд (Next.js)
- `strapi/` — CMS
- `docs/` — проектная документация

## Общение
Пользователь — по-русски. Контент сайта, названия сущностей и UI-строки — по-чешски.

## Конвенции (будут уточнены после утверждения стека)
- Именованные экспорты
- Все пользовательские строки — из Strapi или i18n-словаря, не хардкод
- Секреты (Resend, Ecomail, Strapi token) — только в API routes, никогда в клиентском бандле
